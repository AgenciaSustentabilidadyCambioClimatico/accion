= render 'layouts/remote_form', block: 'convocatoria-create', form_partial: 'form_preparar_convocatoria' # la ultima opción indica el form que se ejecutara, por defecto es form 

/esto no hace nada
/loadScript();