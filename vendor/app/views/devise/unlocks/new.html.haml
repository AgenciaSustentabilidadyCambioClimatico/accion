= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: 'form-devise' }) do |f|
	%h2.form-devise-heading
  	Editar #{resource_name.to_s.humanize}
	= f.input :email, required: true, autofocus: true, placeholder: 'Correo', label: false
	= f.button :submit, "Reenviar", class: 'btn btn-sm btn-primary btn-block'
	= render "devise/shared/links"