:javascript
  $(document).ready(function() {
    $('.chosen-control').chosen({
      allow_single_deselect: true,
      no_results_text: 'No se encontraron resultados',
      width: '100%'
    });
    htmlErrorToTooltip('top');
  });
- titulo t(:manifestación_de_acuerdo)
= simple_form_for @manifestacion_de_interes, url: revisor_manifestacion_de_interes_path(@tarea_pendiente,@manifestacion_de_interes) do |f|
  = f.hidden_field :temporal, as: :boolean, value: true
  =# f.input :temporal, as: :hidden, wrapper: false, input_html: { value: true }
  .row
    .col-12
      .card
        .card-header
          %h5.no-margin
          %b
            =t(:asignar_revisor)
          %h5.no-margin=titulo_instrumento.html_safe
          %h5.no-margin=datos_beneficiario.html_safe
        .card-body
          .row
            .col-12
              = f.input :revisor_id, collection: @manifestacion_de_interes.revisores_select, required: true, input_html: {class: "required-field chosen-control", type: :string, "data-placeholder"=>"Seleccione un revisor"}, label: t(:revisor)
            .col-12
              = f.input :comentario_jefe_de_linea, as: :text, label: t(:comentario), input_html: {class: "required-field"}, required: true
  .float-right
    = link_to t(:back), root_path, class: 'btn btn-sm btn-warning ml-1'
    = button_tag t(:save), 'data-disable-with' => "<i class='fa fa-spinner fa-spin'></i> Guardando...".html_safe, class: "btn btn-sm btn-primary btn-disabled", disabled: :disabled