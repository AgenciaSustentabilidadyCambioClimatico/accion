- titulo t(:lugares)

/ / desde aqui 2018-05-23 DZC
= link_to t(:agregar_, item: t(:lugares).downcase), new_admin_lugar_path, class: 'btn btn-sm btn-primary margin-bottom-20 btn-sm'
.card
	.card-header
		%h5.no-margin
			= t(:buscar_, item: t(:lugares).downcase)
	.card-body
		#lugares-remote-filter
			= render 'filtros'

.card
	.card-header
		%h5.no-margin
			= t(:lista_de_, items: t(:lugares).downcase)
	.card-body
		#lugares-remote-table
			= render 'table'