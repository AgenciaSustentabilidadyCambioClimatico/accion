- titulo t(:dato_levantar)

= link_to t(:agregar_, item: t(:dato_levantar).downcase), new_admin_materia_rubro_relacion_path, class: 'btn btn-sm btn-primary margin-bottom-20 btn-sm'
.card
  .card-header
    %h5.no-margin
      = t(:buscar_, item: t(:dato_levantar).downcase)
  .card-body
    #levantados-remote-filter
      = render 'filtros'

.card
  .card-header
    %h5.no-margin
      = t(:lista_de_, items: t(:dato_levantar).downcase)
  .card-body
    #levantados-remote-table
      = render 'table'