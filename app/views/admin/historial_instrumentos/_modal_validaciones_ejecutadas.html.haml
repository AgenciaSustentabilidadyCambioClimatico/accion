.modal.fade{id: "modal_validaciones_ejecutadas-"+tarea.id.to_s, tabindex: "-1", role: :dialog, "aria-labelledby": :modal1Label, "aria-hidden": true}
  .modal-dialog.modal-dialog-centered.animated{role: :document}
    .modal-content
      .modal-body.p-0
        .card.border-0.m-0
          .card-header
            %h5.m-0=t(:tareas_ejecutadas)
          .card-body.p-0
            %table.table.table-condensed.table-striped.m-0
              %tbody
                -lista.each do |dato|
                  %tr
                    %td=dato[:nombre]
                    -dato[:validaciones].each do |val|
                      %td
                        -if val[:finalizada]
                          =link_to validar_auditorias_manifestacion_de_interes_path(val[:tarea_pendiente_id], manifestacion_de_interes.id), class: "btn-tabla-instrumentos" do
                            =fa_icon 'eye'
                            =val[:nombre]
                        -else
                          %button.btn-tabla-instrumentos{disabled: true, title: "Validación en curso"}
                            =fa_icon 'eye'
                            =val[:nombre]
          .card-footer.text-right
            %button.btn.btn-sm.btn-warning{type: :button, "data-dismiss"=>:modal}
              Volver